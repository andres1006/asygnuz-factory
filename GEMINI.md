# Contexto del proyecto (Gemini / Antigravity)

Reglas y perfiles de agente **no** viven solo en este archivo: la fuente única está en:

- `factory/agents/README.md` — índice de perfiles y herramientas
- `factory/agents/profiles/` — un archivo Markdown por rol
- `factory/agents/handoff-flow.md` — cadena de entregas

Si usas reglas en `.agent/rules/`, mantén **punteros** a esas rutas para no duplicar contenido.
