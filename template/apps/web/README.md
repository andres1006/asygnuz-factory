# App web (Next.js)

Parte del **monorepo del producto**: la raíz del repo tiene `docs/`, `tasks/`, etc.

- **Arranque:** desde la raíz del producto, `pnpm install` y `pnpm dev` (ver `docs/local-development.md`).
- **Stack:** Next.js App Router, Tailwind v4, shadcn/ui, Neon (`src/lib/db.ts`).
